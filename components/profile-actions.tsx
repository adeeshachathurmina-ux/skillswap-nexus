'use client';
import { useState } from 'react';
export function ProfileActions({ profileId }: { profileId: string }) { const [message, setMessage] = useState(''); const act = async (action: string) => { const response = await fetch('/api/social', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action, profileId, reason: action === 'report' ? 'Community report' : undefined }) }); setMessage(response.ok ? `${action} saved` : 'Sign in to use this action'); }; return <div className="mt-8 flex flex-wrap gap-3"><button onClick={() => act('save')} className="rounded-full border border-mint/40 px-4 py-2 text-sm text-mint">Save profile</button><button onClick={() => act('block')} className="rounded-full border border-white/20 px-4 py-2 text-sm text-white/70">Block user</button><button onClick={() => act('report')} className="rounded-full border border-coral/40 px-4 py-2 text-sm text-coral">Report user</button>{message && <span className="self-center text-sm text-white/50">{message}</span>}</div>; }
